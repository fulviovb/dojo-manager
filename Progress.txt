=== PROGRESS: escola-artes-marciais — Sistema de Gestão de Escola de Artes Marciais ===

[T-01] Setup: subir containers Docker e verificar /api/health .............. [x]
[T-02] Auth: login JWT + seed admin@escola.com / 123456 .................... [x]
[T-03] Cadastros base: CRUD Escola, Usuario, ArteMarcial, Faixa, Turma, Sala [x]
[T-04] Engine de aulas: criar Aula automática a partir do HorarioTurma ...... [x]
[T-05] Check-in backend: GET+POST /api/checkin/:qr_token (PRIORIDADE #1) .... [x]
[T-06] Chamada backend: validação professor + fechar chamada ................. [x]
[T-07] Dashboard backend: frequência, graduação, financeiro, alertas ......... [x]
[T-08] Financeiro backend: CRUD planos, mensalidades, pagamentos ............. [x]
[T-09] Frontend: layout, sidebar e login funcional ........................... [x]
[T-10] Frontend: página pública de check-in (/checkin/:qr_token) ............ [x]
[T-11] Frontend: tela de chamada para professor .............................. [x]
[T-12] Frontend: dashboard com semáforo de ausência .......................... [x]
[T-13] Frontend: cadastros (turmas, horários, salas, alunos) ................. [x]
[T-14] Frontend: módulo financeiro ........................................... [x]
[T-15] Frontend: configurações (critérios de graduação, threshold) ........... [x]

Legenda: [ ] pendente | [~] em andamento | [x] concluído | [-] bloqueado

=== FASE 2: refinamentos pós-MVP ===
[x] Chamadas: tela reescrita — lista de aulas (busca + paginação), registro manual
    de aula avulsa, validação individual de check-ins por QR Code (pendente →
    validado_por), remover presença, "marcar todos presentes", link direto pro
    perfil do aluno.
[x] Fechamento de aula: check-ins de QR ainda não revisados são auto-validados
    ao fechar; incremento de aulas_desde_graduacao passou de Promise.all pra
    loop sequencial (Promise.all causava deadlock no MySQL com turmas grandes).
[x] Graduação: cálculo de elegibilidade agora agrupa por (aluno, arte marcial)
    em vez de por matrícula — evita contar dobrado aluno matriculado em duas
    turmas da mesma arte. Contagem de presenças passou a considerar a
    data_inicio da graduação atual (GraduacaoAluno), não mais um contador
    cumulativo (aulas_desde_graduacao) que podia dessincronizar.
[x] Perfil do aluno: histórico de graduação mostra aulas presentes e %
    carência por faixa (usa CriterioGraduacao.min_aulas quando cadastrado);
    suporte a faixa atual em mais de uma arte marcial simultaneamente.
[x] Turmas: ativar/desativar (soft delete) + filtro Ativas/Inativas/Todas;
    edição de professor responsável direto na tela de turma.
[x] Configurações: CRUD de professores (criar, editar, ativar/desativar).
[x] Matrícula: ao reativar matrícula existente, sincroniza graduacao_atual_faixa_id.
[x] Navegação: nome do aluno é clicável em Turmas, Chamadas, Dashboard e
    Financeiro, abrindo o perfil por cima da página atual.
[x] Chamadas: busca por aluno de outra turma/modalidade na tela de detalhe da
    aula, para dar presença a quem treina em turma na qual não está
    matriculado (comum entre alunos buscando carência de graduação).

=== FASE 3: Gestão Financeira completa (planos, assinaturas, faturas) ===
[x] Modelo `AssinaturaAluno`: vínculo aluno↔plano com dia de vencimento e
    status (ativa/pausada/finalizada). `PlanoMensalidade` ganhou
    `periodicidade` (mensal/trimestral/semestral/anual); `Mensalidade`
    ("Fatura" na UI) ganhou `assinatura_id`, `data_vencimento`, `juros`.
[x] `faturaService.js`: geração automática e idempotente de fatura na data de
    vencimento de cada assinatura ativa (mesmo padrão do motor de aulas —
    sem cron, gera o que falta até hoje ao ler endpoints financeiros); lida
    com clamp de fim de mês (dia 31 em fevereiro etc). Geração antecipada
    (`POST /assinaturas/:id/gerar-fatura`) para quem quer pagar antes do
    vencimento normal — idempotente, reaproveita fatura pendente existente.
[x] Pausar assinatura: pula o período pausado (não cobra retroativo); ao
    reativar, recalcula o cursor a partir de hoje só se ele já tiver ficado
    no passado.
[x] Endpoints novos: `/api/assinaturas` (CRUD + pausar/reativar/finalizar/
    gerar-fatura), `/api/mensalidades/:id/cancelar` e `/recibo`,
    `/api/pagamentos/:id` (DELETE = desfazer pagamento), `/api/financeiro/
    painel` (indicadores + série mensal ganhos vs a receber).
[x] Frontend `Financeiro.js` reescrito: abas Painel (dashboard + gráfico) /
    Planos / Assinaturas / Faturas. Modal de pagamento com juros/desconto e
    total devido ao vivo; modal de recibo imprimível (`window.print()`).
[x] `GraficoArea.js`: gráfico de área SVG próprio (sem dependência nova),
    paleta validada pela skill de dataviz do ambiente.
[x] `AlunoDetalhe.js`: seção lateral passou a mostrar os planos vinculados do
    aluno (com pausar/reativar/finalizar) e as últimas faturas, em vez do
    lançamento manual de mensalidade avulsa de antes.
[x] Recibo imprimível ganhou a assinatura de quem administra a escola, lida
    de `frontend/src/assets/assinatura.png`. Esse arquivo é pessoal e fica
    de fora do controle de versão (ver `.gitignore`) — quem clonar o repo
    precisa colocar sua própria imagem de assinatura nesse caminho para o
    recibo renderizar a imagem (sem ela, só falta a imagem; o resto do
    recibo funciona normalmente).
[x] Schema aplicado ao banco existente via script one-off
    (`backend/src/scripts/2026-08-financeiro-schema.js`, já executado) —
    projeto não usa migrations, `sync({alter:false})` não altera tabelas
    já existentes sozinho. Backup do banco feito antes (`backup_pre_financeiro.sql`,
    não versionado).
[x] Recibo passou de modal pra página própria (`ReciboPage.js`, rota
    `/recibo/:id`), aberta em nova aba a partir do botão "Recibo" —
    imprime/salva PDF sem o resto da aplicação por perto (mesmo padrão
    reaproveitado depois pelos Relatórios, ver FASE 4).

=== FASE 4: Relatórios (básicos, inspirados no iDojo) ===
[x] 7 relatórios básicos: Alunos por Graduação, Alunos por Turma-Horário,
    Ficha Cadastral Resumida, Aulas & Frequências (Turma, com modos Relação/
    Quantitativo), Aulas & Frequências (Aluno), Aniversariantes por Mês,
    Frequência: Presença Mínima. Nenhum modelo de banco novo — leitura pura
    sobre tabelas já existentes.
[x] `Relatorios.js`: grid de cards com filtro próprio por relatório (mesmo
    layout do print de referência do iDojo); "Gerar Relatório" abre o
    resultado em nova aba (`/relatorio/:tipo?filtros`), página própria
    imprimível (`RelatorioPage.js`) — mesmo padrão do recibo.
[x] Corrigido bug real encontrado no caminho: `dashboardController.graduacao`
    (elegíveis para graduação, usado no Dashboard e agora também no
    relatório de Presença Mínima) referenciava `grupo.turmaIds`, nunca
    preenchido — a lista de elegíveis provavelmente vinha sempre vazia.
    Aproveitado o mesmo endpoint pra ganhar filtro opcional por arte
    marcial (`?arte_marcial_id=`), evitando duplicar a lógica de
    elegibilidade só pro relatório.

=== FASE 5: Demandas 2026-08-03 (demandas20260803.txt) ===
[x] Histórico de frequência do aluno passou a mostrar também as AUSÊNCIAS, não
    só as presenças. `usuariosController.perfil` ganhou o campo `frequencia`
    (presença + ausência por aula fechada das turmas matriculadas, a partir
    da data_matricula de cada turma — mas presenças reais sempre aparecem,
    mesmo anteriores à matrícula atual, pra não sumir com histórico
    legítimo). Só aulas com `status:'fechada'` contam (mesmo critério do
    semáforo de ausência).
[x] Foto do aluno: upload via `POST /api/usuarios/:id/foto` (multipart,
    `multer`), salva em `backend/uploads/fotos/` (fora do versionamento,
    servido estático em `/uploads`). Campo `foto_url` já existia no model
    e não era usado em lugar nenhum — agora entra em `CAMPOS_ALUNO` e tem
    upload direto no formulário de edição em `Alunos.js`.
[x] Componente `frontend/src/components/Avatar.js` (novo, compartilhado):
    mostra a foto real quando existe, senão iniciais + cor sólida. Usado na
    listagem de `Alunos.js`, na lista de matriculados de `TurmaDetalhe.js`
    (precisou incluir `foto_url` na query de `matriculasController.listar`,
    que não trazia esse campo) e no cabeçalho de `AlunoDetalhe.js` (mantendo
    o gradiente por cor de faixa como fallback, já existente ali).
[x] Cor da faixa nos relatórios: `frontend/src/utils/faixaCores.js` (novo,
    porta a lógica já usada em `TurmaDetalhe.js` — cor do banco quando não é
    o placeholder `#808080`, senão deriva do nome da faixa). Aplicado nos 4
    relatórios que citam faixa (Alunos por Graduação, Alunos por Turma,
    Ficha Cadastral, Presença Mínima); backend passou a enviar `cor`/
    `faixa_cor` nesses payloads (antes buscava e descartava).
[x] Turmas desativadas não aparecem mais nos relatórios: nenhuma query de
    `Turma` em `relatoriosController.js` filtrava `ativa: true` — um aluno
    com matrícula ativa numa turma desativada continuava aparecendo
    agrupado em "Alunos por Graduação", e dava pra gerar "Alunos por
    Turma"/"Frequência (Turma)" passando o `turma_id` de uma turma inativa.
    Agora filtra sempre; turma inativa retorna 404 igual turma inexistente.
[x] Campo de busca em `Relatorios.js` pra filtrar os 7 cards pelo título.

=== FASE 6: Correções no cadastro de aluno ===
[x] Bug: editar um aluno deixando um campo de data em branco (Nascimento ou
    Data de ingresso) quebrava com "Erro interno" — o formulário manda `''`
    e o MySQL rejeita string vazia num campo DATEONLY. Também descoberto que
    o `catch` de `atualizar`/`criar`/`listar`/`buscar`/`desativar` em
    `usuariosController.js` engolia o erro real sem logar nada, por isso não
    aparecia em lugar nenhum — todos os `catch` do arquivo agora fazem
    `console.error(e)`.
[x] Bug relacionado, mais sério: e-mail duplicado ao editar um aluno também
    dava "Erro interno" em vez de mensagem clara — `atualizar` não tratava
    `SequelizeUniqueConstraintError` como `criar` já tratava. Investigando a
    causa raiz de um caso real (duas fichas cadastradas pro mesmo aluno,
    uma ativa com e-mail digitado errado, outra inativa com o e-mail
    certo — a inativa "segurava" o e-mail e travava a correção da ativa)
    ficou claro que o problema de fundo é maior: **crianças pequenas não
    têm e-mail próprio e várias costumam usar o e-mail dos pais**, então
    e-mail nunca deveria ter sido único pra aluno.
[x] CPF passa a ser o identificador único de fato do aluno; e-mail deixou de
    ser único no schema (`backend/src/models/Usuario.js`). E-mail duplicado
    só é bloqueado pra quem realmente loga no sistema (`admin`/`professor`),
    validado na aplicação (`usuariosController.js`), não mais no banco.
    Campos vazios (`cpf`, `data_nascimento`, `data_ingresso`) viram `null`
    antes de salvar — nunca `''`, pra não colidir entre si nos índices
    únicos. Migração one-off já executada
    (`backend/src/scripts/2026-08-cpf-unico.js`): backup do banco, CPFs em
    branco normalizados pra `NULL`, índice único de `email` removido,
    índice único de `cpf` criado (sem duplicatas reais encontradas antes de
    aplicar). O cadastro duplicado real encontrado durante essa investigação
    foi excluído do banco (sem histórico vinculado — chamadas, matrículas,
    financeiro — então seguro remover).

=== BUGS CONHECIDOS ===
[ ] Frontend (dev, React 18 StrictMode): warning "destroy is not a function"
    no console ao desmontar componente na tela de Chamadas. Não observado
    impacto funcional até agora — investigar se persistir em produção.

=== NOTAS ===
- Handoff: ~/escola-manager-discovery/handoffs/2026-05-19-escola-artes-marciais/handoff.md
- GitHub: github.com/fulviovb/dojo-manager (branch padrão: master). O repo já
  existia com um scaffold Express/EJS abandonado (commit único, 10/06/2026,
  nunca era esse projeto de verdade) — esse checkout local começou como
  `git init` sem remoto e só foi conectado ao repo em 04/08/2026. Se um
  clone novo deste projeto não tiver `origin` configurado, é isso: rodar
  `git remote add origin https://github.com/fulviovb/dojo-manager.git`.
- Repos: backend/, frontend/
- Tipo: Produto Novo | Tamanho: G
- Referência de stack: ~/competicao-manager/
- Check-in é PRIORIDADE #1 — começar por T-01 → T-02 → T-04 → T-05 → T-10
- MVP (T-01 a T-15) concluído; trabalho atual é refinamento pós-MVP (ver FASE 2 a FASE 6)
