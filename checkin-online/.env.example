PORT=4000
# Chave secreta compartilhada com o sistema local (CHECKIN_ONLINE_API_KEY no
# backend/.env dele) — protege só as rotas /sync/*, não o check-in público.
API_KEY=troque-por-uma-chave-secreta-longa
