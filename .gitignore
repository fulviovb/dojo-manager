node_modules/
npm-debug.log*

# Env / segredos
.env
.env.local
.env.*.local

# Build
frontend/build/
backend/dist/

# Logs
*.log

# Backups de banco (podem conter dados reais de alunos)
backup_*.sql

# Fotos de alunos enviadas via upload (dados de usuário, não código)
backend/uploads/fotos/*
!backend/uploads/fotos/.gitkeep

# Assinaturas de escola desenhadas via Configurações (dados de usuário)
backend/uploads/assinaturas/*
!backend/uploads/assinaturas/.gitkeep

# Assinatura pessoal usada no recibo — não deve ir para o repositório
frontend/src/assets/assinatura.png
gestao-financeira/assinatura.png
gestao-financeira/assinatura.jpg
gestao-financeira/assinatura2.png

# SO / editores
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code — configuração local do usuário (não compartilhar)
.claude/settings.local.json
